-- Application commands migrated from Modules/programs.conf.
return {
  programs = {
    terminal = "kitty", file_manager = "nautilus", menu = "rofi -show drun",
    browser = "zen-browser", discord = "discord", neovim = "kitty -e nvim",
    yazi = "kitty -e yazi", fyzenor = "kitty -e fyzenor",
  },
}
