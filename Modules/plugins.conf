# .config/hypr/hyprland.conf
plugin {
    hyprexpo {
        columns = 3
        gap_size = 10
        bg_col = $background
        workspace_method = center current

        gesture_distance = 300
    }
}
