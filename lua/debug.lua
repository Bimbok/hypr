-- Modules/debug.conf was not sourced by the former hyprland.conf. It is kept
-- as a Lua module for completeness, but remains unloaded to preserve behavior.
hl.config({ debug = { disable_vfr = false } })
